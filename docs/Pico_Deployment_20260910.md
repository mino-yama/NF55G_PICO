# Pico deployment and integrated smoke test — 2026-09-10

Operator confirmed NF55G disconnected and permitted removal of existing Pico files.
COM14 required elevated access. Read-only inventory found MicroPython 1.28.0,
Pico 2 RP2350, unique ID `2d35a1e11ac125e3`, and only `/sd` in the internal root.
There were no existing application files to back up or delete. Existing SD data was retained.

Deployed all 22 `src/*.py` files to `/src/`. After the finite manual smoke test
passed, copied `firmware/main.py` to `/main.py`. All 23 SHA-256 digests matched
the host source. The device was left stopped in REPL; reset/power-cycle automatic
startup has not yet been exercised.

## Executed checks

`scripts/pico_runtime_smoke.py` runs the deployed package on Pico with real UART
initialization, RTC, clock, SPI SD driver, FAT sink and scheduler. ATE input is an
in-memory peer; these results do not certify the physical ATE UART connection.
No NF55G command was requested; observed NF55G transport writes were zero bytes.

- RTC advanced `20260910_094305` to `20260910_094306`; `RTC_CHECK?` returned `OK`.
- Fragmented CRLF requests produced replies only after LF, with CRLF response termination.
- `*IDN?` returned `NF55G_PICO_FIXTURE,Rev.0`.
- `FW_UPDATE` returned `ERR:FU_DISABLED`.
- `SD_STATUS?`, `TEST_START`, and `TEST_END` returned `OK`.
- FAT capacity reported 15,910,043,648 bytes.
- `/sd/BANK_A/20260910_094306.csv`: header plus 64 rows, exact row sequence, zero drops.
- SD reinitialization/remount and subsequent 65-line readback passed.
- Five scheduler iterations through `run()` followed by `stop()` returned normally.
- File close, unmount and hardware UART deinitialization completed; process exit code 0.

The first smoke attempt also passed the data checks, but its cleanup incorrectly
called `deinit()` on the transport wrapper. The script now calls the underlying
hardware UART; the complete rerun above passed. The first CSV was retained.

Local raw evidence (ignored `temp/`): `pico_runtime_smoke_20260910.log` and
`pico_deployed_inventory_20260910.log`.

## Remaining checks

Cold power-on SD initialization and root automatic startup; physical ATE UART
request/reply; actual NF55G D1/D5 and EB timing; SD fault/removal behavior; and
physical GP4/GP5 separation inspection remain pending. NF55G remains disconnected.
The previous `CMD0 failed: 31` cause is not established. No Open Issue is closed.
Synchronous SD-call latency during newly arriving ATE/EB traffic still needs measurement.


## 2026-09-10: Operator power-cycle check

After the operator reported power cycling, COM14 was inspected without a soft
reset or SD reinitialization. Passive USB capture was empty. Ctrl-C produced a
traceback through `/main.py` -> `src.main.main()` -> `run()` -> `service_once()`
-> ATE receive pumping, proving automatic application startup reached the loop.
This KeyboardInterrupt was deliberately generated by the inspection.

Cold-start SD verification FAILED: `/sd` was empty and statvfs reported the
internal 3 MiB filesystem (768 x 4096 bytes), not the SD filesystem. Reading the
previous CSV returned ENOENT. The startup code catches mount failures, so the
original SD initialization exception was not retained; CMD0 failure or contact
trouble cannot be concluded from this check.

An explicit manual PicoSDSink mount retry then succeeded (15,910,043,648 bytes).
The probe was unmounted and a fresh FixtureApp was created: SD status OK,
RTC_CHECK OK, RTC 20260910_094726. Its run loop was started and the USB connection
closed without interrupting it. No NF55G command was requested by the inspection.
Current state: application running after manual restart, RTC/SD OK at restart.
Cold-start SD initialization remains unresolved; successful retry does not pass
the cold-start requirement. No runtime source or device files were changed.

Local evidence: `temp/pico_power_on_20260910.log`,
`temp/pico_sd_restart_20260910.log`. No Host tests rerun for this hardware-only
inspection and documentation update.
